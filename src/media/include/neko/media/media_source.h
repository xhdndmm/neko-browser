#pragma once

// MediaSource is the video demux/decode seam (formerly a NOT IMPLEMENTED
// stub).  The real interface lives in video.h alongside the FFmpeg wrapper;
// this header is kept as the module's canonical entry point.
#include "neko/media/video.h"
